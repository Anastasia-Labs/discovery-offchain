{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "install-packages": "cd .. && npm run repack-tsc && cd test && rm -rf node_modules && rm -f package-lock.json && npm i",
    "test": "npm run install-packages && npm run vitest",
    "vitest": "export NODE_ENV='emulator' && vitest run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.4",
    "vitest": "^0.30.1"
  },
  "dependencies": {
    "price-discovery-offchain": "file:../price-discovery-offchain-0.0.7.tgz"
  }
}
